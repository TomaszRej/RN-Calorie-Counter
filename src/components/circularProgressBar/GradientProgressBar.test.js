import React from 'react';
import {shallow} from 'enzyme';
import GradientProgressBar from './GradientProgressBar';


describe('gradient bar', () => {
  it('should have correct width', () => {

    const component = shallow(<GradientProgressBar/>);

  });


});
